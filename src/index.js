//import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
//import 'bootstrap/dist/css/bootstrap.min.css'; commented out for now after failed build.

ReactDOM.render(<App />, document.getElementById('root'));


//boilerplate(?)

//the index.html found in the public folder was taken directly from the React repo on GitHub.
//it was added after a failed build.
//this file was moved from the components folder to src after a failed build.

//use npm run start to start development server.

// first test run: 
/* 

navigation works, project errored out. message has no textarea in contact.

*/
